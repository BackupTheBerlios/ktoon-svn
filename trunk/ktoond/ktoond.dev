#!/bin/bash

export LD_LIBRARY_PATH=$KTOON_HOME/lib
export QT_NO_GLIB=1

BASE=`dirname $0`

if [ "$1" == "-g" ]
then
	${BASE}/bin/ktoond.bin $*
else
	shift 1
	gdb -ex run -ex bt --batch --args ${BASE}/bin/ktoond.bin "$*"
fi


